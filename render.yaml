services:
  - type: web
    name: powerhouse-ai-app
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npx tsx server.ts"
    envVars:
      - key: NODE_VERSION
        value: 20
